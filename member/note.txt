// 리눅스에서 프로필 사진 업로드 기능 디버깅 코드
echo '<pre>';
var_dump($_FILES);

$upload_dir = "./profile_upload/";
echo "upload_dir realpath: " . realpath($upload_dir) . PHP_EOL;

if (!is_dir($upload_dir)) {
    die("디렉터리 없음");
}

if (!is_writable($upload_dir)) {
    die("쓰기 권한 없음");
}

if (!move_uploaded_file($_FILES['profile_img']['tmp_name'], $upload_dir.'test.jpg')) {
    die("move_uploaded_file 실패");
}

echo "업로드 성공";
exit;